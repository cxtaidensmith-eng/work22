OVR-Aligned Shared-Query Pool v1 实现与 3 Epoch Smoke Test 总结
日期：2026-08-05

状态：S0、S1、S2 均完成 CUDA 3 epoch smoke；未运行 400 epoch 正式实验。

一、实验隔离
branch=experiment/ovr-aligned-shared-query-v1
worktree=D:\Work\WORK2 final\WORK2 final26.7,21\tmp\ovr_aligned_shared_query_v1
output_root=experiments/ovr_aligned_shared_query_v1/smoke
历史分支、历史模型和历史实验结果均未覆盖。

二、最终实现
1. 三个独立 Per_Label_Pool 替换为一个共享 Per_Label_Pool；共享 learnable Query、MHA、输出投影和 FFN。
2. 三个原始 OVR auxiliary heads 保持独立，loss 仍为 L_main + L_AD-rest + L_CN-rest + L_sMCI-rest。
3. OVR方向 p_k=normalize(stopgrad(W_positive-W_rest))；训练后更新 EMA。
4. EMA方向先跨类别中心化，再以三方向的全局 RMS 归一化。
5. q_effective,k=q_shared + alpha*d_k；alpha 是唯一新增可学习标量，初始化为0。
6. 没有 adapter、新 attention、新 graph、新 loss、routing 或 ensemble。

三、三臂可辨识对照
S0=no anchor：检验共享 Query Pool 本身。
S1=cyclic wrong anchor [1,2,0]：参数完全相同，只打乱类别对应关系。
S2=correct anchor [0,1,2]：检验正确 OVR 对齐是否优于错误对齐。
smoke 为覆盖激活路径使用 warmup=1；未来 formal 固定 warmup=20。

四、参数与初始化公平性
baseline parameters=853131
candidate parameters=703372
delta=-149759 (-17.55%)
candidate state tensors=138
common tensor count=134
baseline common hash=ce26424e6d3c46b25b2167e5abdd4855b3267a589c043cc2ab0983c877b93943
candidate common hash=ce26424e6d3c46b25b2167e5abdd4855b3267a589c043cc2ab0983c877b93943
max_abs_diff=0.0
mismatch_names=[]
fresh construction only；未复制 state，未加载 checkpoint。

五、Smoke 概要（指标仅用于运行检查）
S0 (S0 shared Query Pool without OVR anchor):
  PASS=True; best_epoch=1; ACC=0.35; Macro-F1=0.1728395061728395; BACC=0.3333333333333333; Macro-AUC=0.7819766247533212
  final alpha=0.0; active=False; contribution_norms=[0.0, 0.0, 0.0]
  artifacts=15/15
S1 (S1 shared Query Pool with cyclicly mismatched OVR anchors):
  PASS=True; best_epoch=1; ACC=0.35; Macro-F1=0.1728395061728395; BACC=0.3333333333333333; Macro-AUC=0.7819766247533212
  final alpha=-0.015130679123103619; active=True; contribution_norms=[0.014560445211827755, 0.015077820979058743, 0.01573099009692669]
  artifacts=15/15
S2 (S2 shared Query Pool with correctly aligned OVR anchors):
  PASS=True; best_epoch=1; ACC=0.35; Macro-F1=0.1728395061728395; BACC=0.3333333333333333; Macro-AUC=0.7819766247533212
  final alpha=0.014631656929850578; active=True; contribution_norms=[0.015212169848382473, 0.0140802301466465, 0.014580542221665382]
  artifacts=15/15

六、Smoke 判定
- 三臂 forward、原 OVR loss、backward、EMA、checkpoint 回读和 prediction 重算全部通过。
- S0 的 alpha 梯度与 Query 差异保持为0；S1/S2 在 warm-up 后 alpha 梯度非零且三个有效 Query 可分。
- 强制非训练审计证明 no/wrong/correct 三条计算路径可区分，且审计后初始 state hash 完全恢复。
- 3 epoch 指标不能用于选择 S2，也不能形成论文性能结论。

七、进入正式实验前的建议
Smoke 技术验证已经通过，但当前 formal 状态保持 HOLD；先完成下面的正式运行门禁，再请求人工确认。
正式实验使用 warmup=20，历史 best epoch 规则 ACC > Macro-AUC > Macro-F1 保持不变。
只有当 S2 明显优于参数匹配的 S1，且 alpha/anchor contribution 非零时，才能支持‘正确 OVR 对齐有效’。
若 S2≈S1，只能说明共享池或参数共享有效，不能声称类别语义绑定。
若最佳 epoch≤20，则该最佳结果来自 warm-up 阶段，也不能归因于 OVR anchor。

八、最终只读复核补充（formal 前必须完成）
1. Smoke checkpoint 已在“按 summary 中 arm/epoch 恢复 anchor_active”后实现 logits max_abs_diff=0；但裸 state_dict 不包含 Python 状态 anchor_active。Formal checkpoint 必须保存 mode、epoch、warmup、active metadata，并提供唯一恢复函数（或把 active 状态持久化），再增加无需人工猜测状态的复算门禁。
2. Formal runner 必须实际读取并验证历史 lineage/data，而不只是把 hash 写成常量：
   baseline manifest file SHA256=78114ce1b9cdff40d2c00c9546cb92b79d446d6212b19c0567c989c43a17327d
   baseline manifest canonical SHA256=930288891ead11b143c3485424c5f141cbcd69a3ec7e4a5d72bd7d8076398926
   feature CSV SHA256=2f8efe85c2154d785dc361bc60553c9d983ac1990cee13e38b935b4623787042
   modal dict SHA256=5e72aa0b9268b54e3f447059a728eed96696c97f5783184ffb0c615102f90273
   fold manifest SHA256=0f8964a2009a3660d76c99fa9446f9147a51630083a79d11437a31544438c106
3. Formal EMA 时序固定为：epoch 1–19 不更新；epoch 20 optimizer step 后初始化 EMA；epoch 21 首次激活；epoch 400 后 updates=381。Smoke 为覆盖路径使用 warmup=1，不能照搬其 update count。
4. Candidate 同时引入“共享 Query Pool”和“OVR anchor”。Formal 必须完整运行 S0→S1→S2，不能只跑 S2：Original Query vs S0 判断参数共享；S0 vs S1 判断任意 anchor 影响；S1 vs S2 才判断正确语义对齐。
5. fold0/test-fold best epoch 仍沿用历史探索性协议；结果不得表述为无偏泛化估计。

停止：未启动 400 epoch、10-fold、ensemble 或后续模型。
